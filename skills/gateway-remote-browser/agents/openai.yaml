interface:
  display_name: "Access Gateway Remote Browser"
  short_description: "Acquire and manage an agent remote browser"
  default_prompt: "Use $gateway-remote-browser to acquire a remote browser session."
